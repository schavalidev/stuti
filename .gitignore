.DS_Store
.claude/
builds/
.netlify/
stuti-app/.netlify/

# regenerated by stuti-app/tools/codemod/run-all.sh
stuti-app/tools/codemod/registry.json
stuti-app/tools/codemod/manifest.json
.venv/
stuti-app/.venv/

# the designer's whole Claude Design project, dropped in for a port; the handoff is what gets committed
Stuti_design_*/
